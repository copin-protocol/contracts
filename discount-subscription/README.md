# DCP Gains Rebate

This project contains the smart contract that uses for the referral rebate fee program".

Try running some of the following tasks:

```shell
npx hardhat help
npx hardhat test
REPORT_GAS=true npx hardhat test
npx hardhat node
```
